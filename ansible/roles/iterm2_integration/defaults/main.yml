---

iterm2_integration_users:
  - { name: "debian", home_path: "/home/debian", owner: "debian", group: "debian" }

# Top-level dotfiles copied one by one. Everything under `files/.iterm2/` is no
# longer listed here — that whole tree is transferred with a single rsync (see
# tasks/iterm2_files.yml), so the directory itself is the source of truth.
#
# Note that the old list had already drifted from disk: `files/.iterm2/it2cat`
# exists but was never listed, so it was never deployed. Syncing the tree
# deploys it, which is why the first apply after this change reports `changed`.
iterm2_integration_dotfiles:
  - .iterm2_shell_integration.zsh
  - .iterm2_shell_integration.bash
