# {{ ansible_managed }}
# RCON passwords, read automatically by `docker compose` from the project
# directory. Kept out of docker-compose.yml so that file can stay world-readable
# and its diff reviewable during --check.
{% for mc in minecraft_server_instances %}
{{ mc.name | upper }}_RCON_PASSWORD={{ mc.rcon_password }}
{% endfor %}
